#!/usr/bin/env bash
# This script kills the 7-highlander process and confirms termination

pkill -f '7-highlander' >/dev/null 2>&1
sleep 1

if ! pgrep -f '7-highlander' >/dev/null; then
    echo "OK"
fi
