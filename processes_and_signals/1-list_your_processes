#!/usr/bin/env bash

# This script displays a list of currently running processes in a user-oriented format, including all users and process hierarchy.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,command --sort=user | less
