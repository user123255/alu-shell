#!/usr/bin/env bash
# This script displays all running processes for all users in a user-oriented format, showing the process hierarchy

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
