#!/usr/bin/env bash
# This script displays all running processes for all users
# in a user-oriented format with a hierarchical (tree-like) view.

ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --forest
