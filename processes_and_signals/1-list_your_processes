#!/usr/bin/env bash
# This script displays a list of currently running processes

ps -e --forest -o user=,pid=,tty=,stat=,start=,time=,command= | awk '{if (NR==1) print; else printf "%s %s %s %s %s %s %s\n", $1, $5, $6, $7, $8, $9, $10}' | sed 's/^/    /'
